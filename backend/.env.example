PORT=4000
CORS_ORIGIN=http://localhost:4000

# SMTP — оставьте пустым для dev-режима (письма будут логироваться в консоль)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Плюши <noreply@plushi.local>"
SHOP_EMAIL=orders@plushi.local
